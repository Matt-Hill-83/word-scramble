local module = {}

local config = {
    {
        words = {

            "SAT", --
            "RAT", --
            "CAT", --
            "HAT", --
            "MAT", --
            "PAT", --
            "VAT" --
        }
    }
}

module.config = config
-- module.sectorConfig = sectorConfigs

return module
